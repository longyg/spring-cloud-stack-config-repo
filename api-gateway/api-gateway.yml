zuul:
    #prefix: /api
    ignored-services: "*"
    routes:
        auth-server:
            path: /auth/**
    sensitiveHeaders: Cookie, Set-Cookie

hystrix:
    command:
        default:
            execution.isolation.thread.timeoutInMilliseconds: 5000